import { IMinimalBaseAudioContext, IWorkletOptions } from './interfaces';
export declare const addAudioWorkletModule: (context: IMinimalBaseAudioContext, moduleURL: string, options?: IWorkletOptions) => Promise<void>;

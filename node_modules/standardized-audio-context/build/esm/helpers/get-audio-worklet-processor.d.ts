import { IAudioNode, IAudioWorkletProcessor } from '../interfaces';
export declare const getAudioWorkletProcessor: (offlineAudioContext: OfflineAudioContext, proxy: IAudioNode) => Promise<IAudioWorkletProcessor>;

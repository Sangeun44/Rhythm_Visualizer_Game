export const wrapAudioScheduledSourceNodeStopMethodNegativeParameters = (audioScheduledSourceNode) => {
    audioScheduledSourceNode.stop = ((stop) => {
        return (when = 0) => {
            if (when < 0) {
                throw new RangeError("The parameter can't be negative.");
            }
            stop.call(audioScheduledSourceNode, when);
        };
    })(audioScheduledSourceNode.stop);
};
//# sourceMappingURL=/build/es2015/wrappers/audio-scheduled-source-node-stop-method-negative-parameters.js.map